{"id":"ZPRAC_CDS_JSON_XML","title":"CDS JSON & XML Functions — Data serialization and transformation","phase":9,"phaseName":"Advanced Data Modeling","category":"Advanced Data Modeling","subcategory":9,"language":"abap","description":"CDS JSON & XML Functions — Data serialization and transformation. Key topics: CDS provides built-in functions to serialize entity data into JSON. `with column format json` in the SELECT list serialises a column. `with column format xml` serialises a column value as XML — used.","sections":[{"title":"Key Learning Points","description":"<div class=\"text-slate-600 dark:text-slate-400 text-sm leading-relaxed\"><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>CDS provides built-in functions to serialize entity data into JSON</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>`with column format json` in the SELECT list serialises a column</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>`with column format xml` serialises a column value as XML — used</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>`JSON_VALUE( source_column, '$.path' )` extracts a scalar value</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>`XMLTABLE` inside AMDP transforms XML documents into relational</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>CDS views can consume JSON/XML via association to custom entities</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>Semantic annotations like `@Semantics.json.enabled: true` flag</span></div></div>"},{"title":"CDS Source Code","language":"abap","codeBlock":"/*\n * ====================================================================\n * CDS — View with JSON Column Format\n * ====================================================================\n */\n\n@EndUserText.label: 'Practice CDS — JSON Serialization'\ndefine view ZPRAC_CDS_JSON_SAMPLE\n  as select from /demo/cds_json_source\n{\n  key id            as SourceId,\n      payload       as OriginalPayload,\n      with column format json:\n        original_payload as JsonPayload\n}\nwhere id = 'SAMPLE_001'\n\n/*\n * ====================================================================\n * CDS — Extracting JSON Value with JSON_VALUE\n * ====================================================================\n */\n\n@EndUserText.label: 'Practice CDS — JSON Value Extraction'\ndefine view ZPRAC_CDS_JSON_EXTRACT\n  as select from ZPRAC_CDS_JSON_SAMPLE\n{\n  key SourceId,\n      JsonPayload,\n      JSON_VALUE( JsonPayload, '$.customer.name' )\n        as CustomerName,\n      JSON_VALUE( JsonPayload, '$.order.total' )\n        as OrderTotal\n}\n\n/*\n * ====================================================================\n * CDS — View with XML Column Format\n * ====================================================================\n */\n\n@EndUserText.label: 'Practice CDS — XML Serialization'\ndefine view ZPRAC_CDS_XML_SAMPLE\n  as select from /demo/cds_xml_source\n{\n  key doc_id          as DocumentId,\n      content         as RawContent,\n      with column format xml:\n        content       as XmlContent\n}\n\n/*\n * ====================================================================\n * CDS — Custom Entity for API Payload Parsing\n * ====================================================================\n */\n\n@EndUserText.label: 'Practice CDS — JSON Payload Custom Entity'\n@ObjectModel.query.implementedBy: 'ABAP:ZCL_PRAC_JSON_PARSER'\n\ndefine custom entity ZPRAC_CDS_JsonPayload\n{\n  key payload_id : abap.char(36);\n      source     : abap.char(20);\n      json_body  : abap.string;\n}\n\n/*\n * ====================================================================\n * ABAP — JSON Parser in Custom Entity Implementation\n * ====================================================================\n */\n\nCLASS zcl_prac_json_parser DEFINITION PUBLIC FINAL CREATE PUBLIC.\n  PUBLIC SECTION.\n    INTERFACES if_rap_query_provider.\nENDCLASS.\n\nCLASS zcl_prac_json_parser IMPLEMENTATION.\n\n  METHOD if_rap_query_provider~select.\n    \" Fetch raw JSON from external API (simulated)\n    DATA(raw_json) = |{ \"items\": [{ \"id\": \"1\", \"name\": \"A\" }] }|.\n\n    \" Parse JSON using ABAP JSON library\n    DATA(json_reader) = cl_abap_json=>create_with_parsing( raw_json ).\n    DATA(parsed_data) = VALUE ztab_json_items( ).\n    json_reader->to_abap( IMPORTING data = parsed_data ).\n\n    io_response->set_data( REF #( parsed_data ) ).\n    io_response->set_total_number_of_records( lines( parsed_data ) ).\n  ENDMETHOD.\n\nENDCLASS."}],"content":"<h2 id=\"section-ZPRAC_CDS_JSON_XML-key-learning-points\">Key Learning Points</h2>\n<div class=\"text-slate-600 dark:text-slate-400 text-sm leading-relaxed\"><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>CDS provides built-in functions to serialize entity data into JSON</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>`with column format json` in the SELECT list serialises a column</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>`with column format xml` serialises a column value as XML — used</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>`JSON_VALUE( source_column, '$.path' )` extracts a scalar value</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>`XMLTABLE` inside AMDP transforms XML documents into relational</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>CDS views can consume JSON/XML via association to custom entities</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>Semantic annotations like `@Semantics.json.enabled: true` flag</span></div></div>\n<h2 id=\"section-ZPRAC_CDS_JSON_XML-cds-source-code\">CDS Source Code</h2>\n<pre><code class=\"language-abap\">/*\n * ====================================================================\n * CDS — View with JSON Column Format\n * ====================================================================\n */\n\n@EndUserText.label: 'Practice CDS — JSON Serialization'\ndefine view ZPRAC_CDS_JSON_SAMPLE\n  as select from /demo/cds_json_source\n{\n  key id            as SourceId,\n      payload       as OriginalPayload,\n      with column format json:\n        original_payload as JsonPayload\n}\nwhere id = 'SAMPLE_001'\n\n/*\n * ====================================================================\n * CDS — Extracting JSON Value with JSON_VALUE\n * ====================================================================\n */\n\n@EndUserText.label: 'Practice CDS — JSON Value Extraction'\ndefine view ZPRAC_CDS_JSON_EXTRACT\n  as select from ZPRAC_CDS_JSON_SAMPLE\n{\n  key SourceId,\n      JsonPayload,\n      JSON_VALUE( JsonPayload, '$.customer.name' )\n        as CustomerName,\n      JSON_VALUE( JsonPayload, '$.order.total' )\n        as OrderTotal\n}\n\n/*\n * ====================================================================\n * CDS — View with XML Column Format\n * ====================================================================\n */\n\n@EndUserText.label: 'Practice CDS — XML Serialization'\ndefine view ZPRAC_CDS_XML_SAMPLE\n  as select from /demo/cds_xml_source\n{\n  key doc_id          as DocumentId,\n      content         as RawContent,\n      with column format xml:\n        content       as XmlContent\n}\n\n/*\n * ====================================================================\n * CDS — Custom Entity for API Payload Parsing\n * ====================================================================\n */\n\n@EndUserText.label: 'Practice CDS — JSON Payload Custom Entity'\n@ObjectModel.query.implementedBy: 'ABAP:ZCL_PRAC_JSON_PARSER'\n\ndefine custom entity ZPRAC_CDS_JsonPayload\n{\n  key payload_id : abap.char(36);\n      source     : abap.char(20);\n      json_body  : abap.string;\n}\n\n/*\n * ====================================================================\n * ABAP — JSON Parser in Custom Entity Implementation\n * ====================================================================\n */\n\nCLASS zcl_prac_json_parser DEFINITION PUBLIC FINAL CREATE PUBLIC.\n  PUBLIC SECTION.\n    INTERFACES if_rap_query_provider.\nENDCLASS.\n\nCLASS zcl_prac_json_parser IMPLEMENTATION.\n\n  METHOD if_rap_query_provider~select.\n    \" Fetch raw JSON from external API (simulated)\n    DATA(raw_json) = |{ \"items\": [{ \"id\": \"1\", \"name\": \"A\" }] }|.\n\n    \" Parse JSON using ABAP JSON library\n    DATA(json_reader) = cl_abap_json=>create_with_parsing( raw_json ).\n    DATA(parsed_data) = VALUE ztab_json_items( ).\n    json_reader->to_abap( IMPORTING data = parsed_data ).\n\n    io_response->set_data( REF #( parsed_data ) ).\n    io_response->set_total_number_of_records( lines( parsed_data ) ).\n  ENDMETHOD.\n\nENDCLASS.</code></pre>","tags":["cds-views"],"details":"<p class=\"mb-2\">or XML format directly within the view definition.</p><p class=\"mb-2\">value as JSON — useful for storing complex data in a single field.</p><p class=\"mb-2\">for backward-compatible integration with older SOAP services.</p><p class=\"mb-2\">from a JSON string; `JSON_TABLE` (via AMDP) expands JSON arrays</p><p class=\"mb-2\">into tabular form.</p><p class=\"mb-2\">rows (requires HANA XML functions).</p><p class=\"mb-2\">or table functions that handle the parsing logic in ABAP/AMDP.</p><p class=\"mb-2\">CDS elements for JSON serialization in OData V4 responses.</p><p class=\"mb-2\">COMPARISON: JSON vs XML in CDS</p><p class=\"mb-2\">Aspect              | JSON Function              | XML Function</p><p class=\"mb-2\">---------------------------------------------------------------------------------------------------------</p><p class=\"mb-2\">Serialize to format | `with column format json`  | `with column format xml`</p><p class=\"mb-2\">OData V4 support    | Native (JSON as default)   | Legacy (SOAP / OData V2)</p><p class=\"mb-2\">Parse / extract     | `JSON_VALUE` expression    | `XMLTABLE` in AMDP</p><p class=\"mb-2\">Array expansion     | JSON_TABLE in HANA AMDP    | XMLTABLE in HANA AMDP</p><p class=\"mb-2\">Annotation          | @Semantics.json.enabled    | @Semantics.xml.enabled</p><p class=\"mb-2\">Integration use     | REST / Fiori / Node.js     | SOAP / PI / Legacy ECC</p><p class=\"mb-2\">Performance         | Fast (native HANA JSON)    | Moderate (XPath parsing)</p>","cdsViewName":"ZPRAC_CDS_JSON_XML","sqlViewName":"","topic":"JSON, XML, serialization, deserialization in CDS views"}