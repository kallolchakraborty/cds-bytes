{"id":"ZPRAC_CDS_EXISTS","title":"EXISTS and NOT EXISTS subqueries in WHERE clause","phase":4,"phaseName":"Aggregation & Subqueries","category":"Aggregation & Subqueries","subcategory":"Subqueries","language":"abap","description":"EXISTS and NOT EXISTS subqueries in WHERE clause. Key topics: EXISTS checks if at least one row exists in the subquery. NOT EXISTS ensures NO rows exist in the subquery. The subquery is CORRELATED — it references outer query fields.","sections":[{"title":"Key Learning Points","description":"<div class=\"text-slate-600 dark:text-slate-400 text-sm leading-relaxed\"><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>EXISTS checks if at least one row exists in the subquery</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>NOT EXISTS ensures NO rows exist in the subquery</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>The subquery is CORRELATED — it references outer query fields</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>EXISTS is often more efficient than LEFT JOIN + NULL check</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>EXISTS stops scanning as soon as one match is found</span></div></div>"},{"title":"CDS Source Code","language":"abap","codeBlock":"@AbapCatalog.sqlViewName: 'ZPRAC_SQL_EXISTS'\n@AbapCatalog.compiler.compareFilter: true\n@AbapCatalog.preserveKey: true\n@AccessControl.authorizationCheck: #CHECK\n@EndUserText.label: 'Practice CDS - EXISTS / NOT EXISTS subqueries'\ndefine view ZPRAC_CDS_EXISTS\n  as select from /DMO/I_Travel_D as _Travel\n{\n  key _Travel.TravelUUID,\n      _Travel.TravelID,\n      _Travel.AgencyID,\n      _Travel.CustomerID,\n      @Semantics.date: true\n      _Travel.BeginDate,\n      @Semantics.date: true\n      _Travel.EndDate,\n      @Semantics.amount.currencyCode: 'CurrencyCode'\n      _Travel.TotalPrice,\n      _Travel.CurrencyCode,\n      @Semantics.amount.currencyCode: 'CurrencyCode'\n      _Travel.BookingFee,\n      _Travel.Description,\n      _Travel.Status\n\n}\nwhere\n  /*\n   * EXISTS: Returns travels that have AT LEAST ONE booking with status 'N' (New).\n   * For each row in _Travel, the subquery checks if a matching booking exists.\n   * As soon as one match is found, EXISTS returns TRUE and moves to next row.\n   */\n  exists (\n    select from /DMO/I_Booking_D as _Booking\n      where _Booking.TravelUUID = _Travel.TravelUUID\n        and _Booking.BookingStatus = 'N'\n  )\n\n/*\n * ALTERNATIVE: NOT EXISTS — uncomment this and comment the EXISTS above:\n *\n *   and not exists (\n *     select from /DMO/I_Booking_D as _Booking\n *       where _Booking.TravelUUID = _Travel.TravelUUID\n *         and _Booking.BookingStatus = 'B'\n *   )\n *\n * NOT EXISTS: Returns travels that have NO 'Accepted' (B) bookings.\n */"}],"content":"<h2 id=\"section-ZPRAC_CDS_EXISTS-key-learning-points\">Key Learning Points</h2>\n<div class=\"text-slate-600 dark:text-slate-400 text-sm leading-relaxed\"><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>EXISTS checks if at least one row exists in the subquery</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>NOT EXISTS ensures NO rows exist in the subquery</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>The subquery is CORRELATED — it references outer query fields</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>EXISTS is often more efficient than LEFT JOIN + NULL check</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>EXISTS stops scanning as soon as one match is found</span></div></div>\n<h2 id=\"section-ZPRAC_CDS_EXISTS-cds-source-code\">CDS Source Code</h2>\n<pre><code class=\"language-abap\">@AbapCatalog.sqlViewName: 'ZPRAC_SQL_EXISTS'\n@AbapCatalog.compiler.compareFilter: true\n@AbapCatalog.preserveKey: true\n@AccessControl.authorizationCheck: #CHECK\n@EndUserText.label: 'Practice CDS - EXISTS / NOT EXISTS subqueries'\ndefine view ZPRAC_CDS_EXISTS\n  as select from /DMO/I_Travel_D as _Travel\n{\n  key _Travel.TravelUUID,\n      _Travel.TravelID,\n      _Travel.AgencyID,\n      _Travel.CustomerID,\n      @Semantics.date: true\n      _Travel.BeginDate,\n      @Semantics.date: true\n      _Travel.EndDate,\n      @Semantics.amount.currencyCode: 'CurrencyCode'\n      _Travel.TotalPrice,\n      _Travel.CurrencyCode,\n      @Semantics.amount.currencyCode: 'CurrencyCode'\n      _Travel.BookingFee,\n      _Travel.Description,\n      _Travel.Status\n\n}\nwhere\n  /*\n   * EXISTS: Returns travels that have AT LEAST ONE booking with status 'N' (New).\n   * For each row in _Travel, the subquery checks if a matching booking exists.\n   * As soon as one match is found, EXISTS returns TRUE and moves to next row.\n   */\n  exists (\n    select from /DMO/I_Booking_D as _Booking\n      where _Booking.TravelUUID = _Travel.TravelUUID\n        and _Booking.BookingStatus = 'N'\n  )\n\n/*\n * ALTERNATIVE: NOT EXISTS — uncomment this and comment the EXISTS above:\n *\n *   and not exists (\n *     select from /DMO/I_Booking_D as _Booking\n *       where _Booking.TravelUUID = _Travel.TravelUUID\n *         and _Booking.BookingStatus = 'B'\n *   )\n *\n * NOT EXISTS: Returns travels that have NO 'Accepted' (B) bookings.\n */</code></pre>","tags":["subqueries"],"details":"<p class=\"mb-2\">EXISTS vs JOIN:</p><p class=\"mb-2\">┌──────────────┬───────────────────────────┬─────────────────────┐</p><p class=\"mb-2\">│ Scenario     │ EXISTS                     │ JOIN                │</p><p class=\"mb-2\">├──────────────┼───────────────────────────┼─────────────────────┤</p><p class=\"mb-2\">│ \"Has at least│ EXISTS + correlated subq  │ JOIN + DISTINCT     │</p><p class=\"mb-2\">│  one XXX\"    │ (stops on first match)    │ (processes all rows)│</p><p class=\"mb-2\">│ \"Has no XXX\" │ NOT EXISTS (can use index) │ LEFT JOIN + NULL    │</p><p class=\"mb-2\">└──────────────┴───────────────────────────┴─────────────────────┘</p><p class=\"mb-2\">USE CASES:</p><p class=\"mb-2\">EXISTS:  Find travels that have at least one 'New' (N) booking</p><p class=\"mb-2\">NOT EXISTS: Find travels that have NO 'Accepted' (B) bookings</p>","cdsViewName":"ZPRAC_CDS_EXISTS","sqlViewName":"ZPRAC_SQL_EXISTS","topic":"Correlated subqueries — existence checks without JOIN"}