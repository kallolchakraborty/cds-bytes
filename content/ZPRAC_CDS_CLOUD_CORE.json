{"id":"ZPRAC_CDS_CLOUD_CORE","title":"CDS in ABAP Cloud & Clean Core Strategy — Modern","phase":12,"phaseName":"Quality, Performance & Enterprise","category":"Quality, Performance & Enterprise","subcategory":"Integration","language":"abap","description":"CDS in ABAP Cloud & Clean Core Strategy — Modern. Key topics: ABAP Cloud development model requires `#CLOUD_DEVELOPMENT` as. Released APIs are marked with `@ABAP.ReleaseState: #RELEASED`. Clean-core extensibility: SAP's standard namespace (/DMO/, /SAP/).","sections":[{"title":"Key Learning Points","description":"<div class=\"text-slate-600 dark:text-slate-400 text-sm leading-relaxed\"><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>ABAP Cloud development model requires `#CLOUD_DEVELOPMENT` as</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>Released APIs are marked with `@ABAP.ReleaseState: #RELEASED`</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>Clean-core extensibility: SAP's standard namespace (/DMO/, /SAP/)</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>Cloud vs on-premise differences: `define view entity` is</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>Extensibility via `@EndUserText.label: 'Custom'` and</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>CDS view extensions (`extends view`) add fields from customer</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>Use ADT's system library (Object Explorer > Properties) to</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>Upgrade-safe custom code means using only released APIs,</span></div></div>"},{"title":"CDS Source Code","language":"abap","codeBlock":"@EndUserText.label: 'Extension for Travel — customer fields'\n@AbapCatalog.enhancement.allowExtend: true\ndefine view entity ZPRAC_TRAVEL_EXTENSION\n  as select from /dmo/travel\n{\n  key travel_id\n}\n// Customer appends fields here — the extension is an additive layer\n// on top of the standard view without modifying it.\n\n/*\n * ---- CDS Append View (customer namespace) ----\n * File: ZPRAC_APPEND_TRAVEL_CUSTOMER.ddls (appended to ZPRAC_TRAVEL_OPTIMIZED)\n *\n *  @EndUserText.label: 'Customer-specific fields for Travel'\n *  extend view entity ZPRAC_TRAVEL_OPTIMIZED with ZPRAC_APPEND_TRAVEL\n *  {\n *    cast( '' as ZCUSTOMER_STATUS_C ) as ZCustomerStatusExt\n *  }\n *\n * ====================================================================\n * 2. RELEASED API ANNOTATION — SYSTEM LIBRARY EXAMPLE\n * ====================================================================\n *\n * The following annotation appears in SAP-delivered CDS views to\n * declare their API contract.  ADT highlights these in the system\n * library browser:\n *\n *  @ABAP.ReleaseState: #RELEASED\n *\n * Use this as a gate: if an object lacks this annotation (or is marked\n * #DEPRECATED / #NOT_RELEASED), it is NOT allowed in ABAP Cloud.\n *\n * ====================================================================\n * 3. CLEAN-CORE COMPLIANT CUSTOM CDS — RELEASED APIS ONLY\n * ====================================================================\n */\n@EndUserText.label: 'Clean-Core Custom Travel Analysis'\n@AccessControl.authorizationCheck: #NOT_REQUIRED\n@AbapCatalog.compiler.compareFilter: true\ndefine root view entity ZPRAC_CLEAN_CORE_TRAVEL\n  as select from /dmo/travel\n{\n  key travel_id,\n      customer_id,\n      agency_id,\n      total_price,\n      currency_code,\n      status,\n      created_at\n}\n/*\n *  This view uses:\n *    - Only released CDS entities from /DMO/ namespace\n *    - No deprecated define view (uses define root view entity)\n *    - No @AbapCatalog.sqlViewName (cloud-declared as deprecated)\n *    - Explicit authorization check annotation\n *    - No direct DB table references — goes through /DMO/travel entity\n *\n *  It is fully compliant with the ABAP Cloud clean-core strategy.\n *\n * ====================================================================\n * 10 STEPS TO CLEAN-CORE CDS DEVELOPMENT — CHECKLIST\n * ====================================================================\n *\n *  #  Step\n * --- ---------------------------------------------------------------\n *  1  SET language version to #CLOUD_DEVELOPMENT in the CDS source\n *     header (or ADT project properties).\n *  2  USE only released CDS entities — verify the release state in\n *     the system library (look for @ABAP.ReleaseState: #RELEASED).\n *  3  DEFINE views as root view entity — never use deprecated\n *     define view.  Always add `root` for top-level views.\n *  4  EXTEND standard views via CDS append views (extend view entity)\n *     instead of modifying SAP objects.  This survives upgrades.\n *  5  ANNOTATE custom CDS with explicit @AccessControl.authorizationCheck\n *     and @EndUserText.label for documentation and governance.\n *  6  AVOID RFC/BAPI calls in CDS or in the underlying RAP behavior —\n *     use released OData or RAP APIs instead.\n *  7  OMIT @AbapCatalog.sqlViewName — it is deprecated in cloud.\n *     Rely on the entity name for SQL consumption.\n *  8  USE adaptive RFC (via released OData services) if you need\n *     remote access — never call BAPIs directly.\n *  9  TEST after every SAP upgrade with ATC — released API contracts\n *     guarantee no breaking changes when the contract is honored.\n * 10  DOCUMENT the clean-core compliance level in the CDS header\n *     (as shown in this file) — mark which APIs are released and\n *     which are customer-only extensions.\n * ====================================================================\n */"}],"content":"<h2 id=\"section-ZPRAC_CDS_CLOUD_CORE-key-learning-points\">Key Learning Points</h2>\n<div class=\"text-slate-600 dark:text-slate-400 text-sm leading-relaxed\"><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>ABAP Cloud development model requires `#CLOUD_DEVELOPMENT` as</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>Released APIs are marked with `@ABAP.ReleaseState: #RELEASED`</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>Clean-core extensibility: SAP's standard namespace (/DMO/, /SAP/)</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>Cloud vs on-premise differences: `define view entity` is</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>Extensibility via `@EndUserText.label: 'Custom'` and</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>CDS view extensions (`extends view`) add fields from customer</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>Use ADT's system library (Object Explorer > Properties) to</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>Upgrade-safe custom code means using only released APIs,</span></div></div>\n<h2 id=\"section-ZPRAC_CDS_CLOUD_CORE-cds-source-code\">CDS Source Code</h2>\n<pre><code class=\"language-abap\">@EndUserText.label: 'Extension for Travel — customer fields'\n@AbapCatalog.enhancement.allowExtend: true\ndefine view entity ZPRAC_TRAVEL_EXTENSION\n  as select from /dmo/travel\n{\n  key travel_id\n}\n// Customer appends fields here — the extension is an additive layer\n// on top of the standard view without modifying it.\n\n/*\n * ---- CDS Append View (customer namespace) ----\n * File: ZPRAC_APPEND_TRAVEL_CUSTOMER.ddls (appended to ZPRAC_TRAVEL_OPTIMIZED)\n *\n *  @EndUserText.label: 'Customer-specific fields for Travel'\n *  extend view entity ZPRAC_TRAVEL_OPTIMIZED with ZPRAC_APPEND_TRAVEL\n *  {\n *    cast( '' as ZCUSTOMER_STATUS_C ) as ZCustomerStatusExt\n *  }\n *\n * ====================================================================\n * 2. RELEASED API ANNOTATION — SYSTEM LIBRARY EXAMPLE\n * ====================================================================\n *\n * The following annotation appears in SAP-delivered CDS views to\n * declare their API contract.  ADT highlights these in the system\n * library browser:\n *\n *  @ABAP.ReleaseState: #RELEASED\n *\n * Use this as a gate: if an object lacks this annotation (or is marked\n * #DEPRECATED / #NOT_RELEASED), it is NOT allowed in ABAP Cloud.\n *\n * ====================================================================\n * 3. CLEAN-CORE COMPLIANT CUSTOM CDS — RELEASED APIS ONLY\n * ====================================================================\n */\n@EndUserText.label: 'Clean-Core Custom Travel Analysis'\n@AccessControl.authorizationCheck: #NOT_REQUIRED\n@AbapCatalog.compiler.compareFilter: true\ndefine root view entity ZPRAC_CLEAN_CORE_TRAVEL\n  as select from /dmo/travel\n{\n  key travel_id,\n      customer_id,\n      agency_id,\n      total_price,\n      currency_code,\n      status,\n      created_at\n}\n/*\n *  This view uses:\n *    - Only released CDS entities from /DMO/ namespace\n *    - No deprecated define view (uses define root view entity)\n *    - No @AbapCatalog.sqlViewName (cloud-declared as deprecated)\n *    - Explicit authorization check annotation\n *    - No direct DB table references — goes through /DMO/travel entity\n *\n *  It is fully compliant with the ABAP Cloud clean-core strategy.\n *\n * ====================================================================\n * 10 STEPS TO CLEAN-CORE CDS DEVELOPMENT — CHECKLIST\n * ====================================================================\n *\n *  #  Step\n * --- ---------------------------------------------------------------\n *  1  SET language version to #CLOUD_DEVELOPMENT in the CDS source\n *     header (or ADT project properties).\n *  2  USE only released CDS entities — verify the release state in\n *     the system library (look for @ABAP.ReleaseState: #RELEASED).\n *  3  DEFINE views as root view entity — never use deprecated\n *     define view.  Always add `root` for top-level views.\n *  4  EXTEND standard views via CDS append views (extend view entity)\n *     instead of modifying SAP objects.  This survives upgrades.\n *  5  ANNOTATE custom CDS with explicit @AccessControl.authorizationCheck\n *     and @EndUserText.label for documentation and governance.\n *  6  AVOID RFC/BAPI calls in CDS or in the underlying RAP behavior —\n *     use released OData or RAP APIs instead.\n *  7  OMIT @AbapCatalog.sqlViewName — it is deprecated in cloud.\n *     Rely on the entity name for SQL consumption.\n *  8  USE adaptive RFC (via released OData services) if you need\n *     remote access — never call BAPIs directly.\n *  9  TEST after every SAP upgrade with ATC — released API contracts\n *     guarantee no breaking changes when the contract is honored.\n * 10  DOCUMENT the clean-core compliance level in the CDS header\n *     (as shown in this file) — mark which APIs are released and\n *     which are customer-only extensions.\n * ====================================================================\n */</code></pre>","tags":["rap","cloud-core"],"details":"<p class=\"mb-2\">the language version — only released APIs are allowed. The</p><p class=\"mb-2\">compiler enforces this at compile time.</p><p class=\"mb-2\">in the system library (ADT). Using unreleased objects causes</p><p class=\"mb-2\">a syntax error in cloud projects.</p><p class=\"mb-2\">is locked. Extensions go into customer namespace (Z*, Y*) via</p><p class=\"mb-2\">CDS appends, custom fields, custom logic, and RAP BO extensions.</p><p class=\"mb-2\">mandatory in cloud (DDIC-based `define view` is not allowed);</p><p class=\"mb-2\">no direct RFC/BAPI calls; no ABAP SQL in OPEN SQL that bypasses</p><p class=\"mb-2\">CDS; `@AbapCatalog.sqlViewName` is deprecated.</p><p class=\"mb-2\">`@Extensible: true` on CDS views allows customers to append</p><p class=\"mb-2\">fields without modifying the original view.</p><p class=\"mb-2\">tables or custom logic without changing standard CDS — key</p><p class=\"mb-2\">ingredient of the \"clean-core\" approach.</p><p class=\"mb-2\">verify the release state of any API before using it.</p><p class=\"mb-2\">extending via enhancement options, and never modifying SAP</p><p class=\"mb-2\">objects. SAP guarantees backward compatibility of released APIs.</p><p class=\"mb-2\">COMPARISON TABLE: ON-PREMISE CDS vs ABAP CLOUD CDS</p><p class=\"mb-2\">Aspect                   On-Premise CDS               ABAP Cloud CDS</p><p class=\"mb-2\">-----------------------  ---------------------------  ---------------------------</p><p class=\"mb-2\">Language version         Standard ABAP (#STANDARD)    #CLOUD_DEVELOPMENT</p><p class=\"mb-2\">View definition          define view (deprecated)     define root view entity</p><p class=\"mb-2\">DDIC compatibility       CDS DDIC-based views         No DDIC view dependency</p><p class=\"mb-2\">SQL View name            @AbapCatalog.sqlViewName    Deprecated — use entity name</p><p class=\"mb-2\">API usage                Any API                     Only @ABAP.ReleaseState: #RELEASED</p><p class=\"mb-2\">DB hints                 Allowed                     Not allowed in cloud</p><p class=\"mb-2\">Modifications            Can modify SAP objects       Never modify — extend only</p><p class=\"mb-2\">Extensibility            SAP Notes, corrections       Key-user / developer appends</p><p class=\"mb-2\">Deployment               On-prem S/4, BW, etc        S/4HANA Cloud, BTP ABAP</p><p class=\"mb-2\">Transport mechanism      CTS (file-based)            gCTS / abapGit / ADT-managed</p><p class=\"mb-2\">RFC/BAPI                 Allowed                     Not allowed (use RAP / OData)</p><p class=\"mb-2\">Authorization            DCL + SU22                  DCL + IAM apps</p><p class=\"mb-2\">Buffer behavior          Full buffer control         @AccessControl.authorizationCheck</p><p class=\"mb-2\">Upgrade protection       Manual regression           Automatic — released API contracts</p><p class=\"mb-2\">1. CDS VIEW EXTENSION — ADDITIVE EXTENSIBILITY</p>","cdsViewName":"ZPRAC_CDS_CLOUD_CORE","sqlViewName":"","topic":"ABAP Cloud language version, clean-core extensibility,"}