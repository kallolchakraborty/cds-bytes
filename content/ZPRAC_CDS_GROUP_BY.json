{
  "id": "ZPRAC_CDS_GROUP_BY",
  "title": "Aggregation with GROUP BY on Booking data",
  "phase": 4,
  "phaseName": "Aggregation & Subqueries",
  "category": "Aggregation & Subqueries",
  "subcategory": "Aggregation",
  "language": "abap",
  "description": "Aggregation with GROUP BY on Booking data. Key topics: All non-aggregated fields in the SELECT list MUST be in GROUP BY. Available aggregate functions:. Aggregation produces a new semantic layer — @Semantics annotations.",
  "sections": [
    {
      "title": "Key Learning Points",
      "description": "<div class=\"text-slate-600 dark:text-slate-400 text-sm leading-relaxed\"><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>All non-aggregated fields in the SELECT list MUST be in GROUP BY</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>Available aggregate functions:</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>Aggregation produces a new semantic layer — @Semantics annotations</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>GROUP BY fields are implicitly keys of the aggregated result</span></div></div>"
    },
    {
      "title": "CDS Source Code",
      "language": "abap",
      "codeBlock": "@AbapCatalog.sqlViewName: 'ZPRAC_SQL_GROUP_BY'\n@AbapCatalog.compiler.compareFilter: true\n@AbapCatalog.preserveKey: true\n@AccessControl.authorizationCheck: #CHECK\n@EndUserText.label: 'Practice CDS - Aggregation: SUM, COUNT, AVG, MIN, MAX with GROUP BY'\ndefine view ZPRAC_CDS_GROUP_BY\n  as select from /DMO/I_Booking_D\n{\n  /*\n   * Grouping field — all non-aggregated fields must appear here\n   */\n  key CustomerID,\n\n  /*\n   * Aggregate functions\n   * Currency reference field added for @Semantics.amount annotations.\n   * In a real scenario, use COALESCE to handle mixed currencies or filter\n   * to a single currency in WHERE clause.\n   */\n      count(*)                                    as BookingCount,\n      count(FlightPrice)                          as BookingsWithPrice,\n      count(distinct AirlineID)                   as DistinctAirlines,\n\n      @Semantics.amount.currencyCode: 'CurrencyCode'\n      sum(FlightPrice)                            as TotalSpent,\n\n      @Semantics.amount.currencyCode: 'CurrencyCode'\n      avg(FlightPrice)                            as AvgFlightPrice,\n\n      @Semantics.amount.currencyCode: 'CurrencyCode'\n      min(FlightPrice)                            as MinFlightPrice,\n\n      @Semantics.amount.currencyCode: 'CurrencyCode'\n      max(FlightPrice)                            as MaxFlightPrice,\n\n      /*\n       * Currency reference field — all amount annotations reference this.\n       * In production, consider currencies from a single booking or filter\n       * by a specific currency to avoid mixing currency types.\n       */\n      min(CurrencyCode)                           as CurrencyCode\n\n}\ngroup by CustomerID"
    }
  ],
  "content": "<h2 id=\"section-ZPRAC_CDS_GROUP_BY-key-learning-points\">Key Learning Points</h2>\n<div class=\"text-slate-600 dark:text-slate-400 text-sm leading-relaxed\"><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>All non-aggregated fields in the SELECT list MUST be in GROUP BY</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>Available aggregate functions:</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>Aggregation produces a new semantic layer — @Semantics annotations</span></div><div class=\"flex items-start gap-2 mb-1.5\"><span class=\"text-brand-500 mt-0.5 shrink-0\">◆</span><span>GROUP BY fields are implicitly keys of the aggregated result</span></div></div>\n<h2 id=\"section-ZPRAC_CDS_GROUP_BY-cds-source-code\">CDS Source Code</h2>\n<pre><code class=\"language-abap\">@AbapCatalog.sqlViewName: 'ZPRAC_SQL_GROUP_BY'\n@AbapCatalog.compiler.compareFilter: true\n@AbapCatalog.preserveKey: true\n@AccessControl.authorizationCheck: #CHECK\n@EndUserText.label: 'Practice CDS - Aggregation: SUM, COUNT, AVG, MIN, MAX with GROUP BY'\ndefine view ZPRAC_CDS_GROUP_BY\n  as select from /DMO/I_Booking_D\n{\n  /*\n   * Grouping field — all non-aggregated fields must appear here\n   */\n  key CustomerID,\n\n  /*\n   * Aggregate functions\n   * Currency reference field added for @Semantics.amount annotations.\n   * In a real scenario, use COALESCE to handle mixed currencies or filter\n   * to a single currency in WHERE clause.\n   */\n      count(*)                                    as BookingCount,\n      count(FlightPrice)                          as BookingsWithPrice,\n      count(distinct AirlineID)                   as DistinctAirlines,\n\n      @Semantics.amount.currencyCode: 'CurrencyCode'\n      sum(FlightPrice)                            as TotalSpent,\n\n      @Semantics.amount.currencyCode: 'CurrencyCode'\n      avg(FlightPrice)                            as AvgFlightPrice,\n\n      @Semantics.amount.currencyCode: 'CurrencyCode'\n      min(FlightPrice)                            as MinFlightPrice,\n\n      @Semantics.amount.currencyCode: 'CurrencyCode'\n      max(FlightPrice)                            as MaxFlightPrice,\n\n      /*\n       * Currency reference field — all amount annotations reference this.\n       * In production, consider currencies from a single booking or filter\n       * by a specific currency to avoid mixing currency types.\n       */\n      min(CurrencyCode)                           as CurrencyCode\n\n}\ngroup by CustomerID</code></pre>",
  "tags": [
    "cds-views"
  ],
  "details": "<p class=\"mb-2\">┌──────────────────┬────────────────────────────────┐</p><p class=\"mb-2\">│ Function         │ Description                    │</p><p class=\"mb-2\">├──────────────────┼────────────────────────────────┤</p><p class=\"mb-2\">│ COUNT(*)         │ Number of rows in group        │</p><p class=\"mb-2\">│ COUNT( field )   │ Number of non-NULL values      │</p><p class=\"mb-2\">│ COUNT(DISTINCT x)│ Unique non-NULL values         │</p><p class=\"mb-2\">│ SUM( field )     │ Total (numeric only)           │</p><p class=\"mb-2\">│ AVG( field )     │ Average (numeric only)         │</p><p class=\"mb-2\">│ MIN( field )     │ Minimum value                  │</p><p class=\"mb-2\">│ MAX( field )     │ Maximum value                  │</p><p class=\"mb-2\">│ STRING_AGG(...)  │ Concatenate strings (HANA)     │</p><p class=\"mb-2\">└──────────────────┴────────────────────────────────┘</p><p class=\"mb-2\">should be added to aggregated fields for downstream consumption</p><p class=\"mb-2\">USE CASE:</p><p class=\"mb-2\">Per-customer booking summary: how many bookings, total spent,</p><p class=\"mb-2\">average price, cheapest and most expensive flight.</p>",
  "cdsViewName": "ZPRAC_CDS_GROUP_BY",
  "sqlViewName": "ZPRAC_SQL_GROUP_BY",
  "topic": "Aggregate functions — SUM, COUNT, AVG, MIN, MAX, COUNT(DISTINCT)"
}