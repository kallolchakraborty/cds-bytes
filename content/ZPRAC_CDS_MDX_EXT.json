{
  "id": "ZPRAC_CDS_MDX_EXT",
  "title": "Metadata Extension pattern (separating UI annotations)",
  "phase": 7,
  "phaseName": "Fiori & OData",
  "category": "Fiori & OData",
  "subcategory": "Modeling",
  "language": "abap",
  "description": "Metadata Extension pattern (separating UI annotations)",
  "sections": [
    {
      "title": "CDS Source Code",
      "language": "abap",
      "codeBlock": "/*\n * ====================================================================\n * BASE CDS VIEW (with extension enabled)\n * ====================================================================\n */\n@AbapCatalog.sqlViewName: 'ZPRAC_SQL_MDXEXT'\n@AbapCatalog.compiler.compareFilter: true\n@AbapCatalog.preserveKey: true\n@AccessControl.authorizationCheck: #CHECK\n@EndUserText.label: 'Practice CDS - Base view for Metadata Extension'\n@Metadata.allowExtensions: true\ndefine view ZPRAC_CDS_MDX_EXT\n  as select from /dmo/travel\n{\n  @Environment.systemField: #CLIENT\n  key client            as Client,\n  key travel_id         as TravelId,\n      agency_id         as AgencyId,\n      customer_id       as CustomerId,\n      @Semantics.date: true\n      begin_date        as BeginDate,\n      @Semantics.date: true\n      end_date          as EndDate,\n      @Semantics.amount.currencyCode: 'CurrencyCode'\n      booking_fee       as BookingFee,\n      @Semantics.amount.currencyCode: 'CurrencyCode'\n      total_price       as TotalPrice,\n      currency_code     as CurrencyCode,\n      description       as Description,\n      status            as Status\n}\n\n/*\n * ====================================================================\n * METADATA EXTENSION (separate artifact, not inside the view)\n *\n * Create as a separate file: ZPRAC_CDS_MDX_EXT.asr\n * \n * @Metadata.layer: #CORE\n * annotate view ZPRAC_CDS_MDX_EXT with\n * {\n *   @UI.facet: [\n *     {\n *       id: 'TravelFacet',\n *       purpose: #STANDARD,\n *       type: #IDENTIFICATION_REFERENCE,\n *       label: 'Travel Details'\n *     }\n *   ]\n *   TravelId;\n *\n *   @UI.lineItem: [ { position: 10, label: 'Travel ID' } ]\n *   @UI.identification: [ { position: 10 } ]\n *   TravelId;\n *\n *   @UI.lineItem: [ { position: 20, label: 'Total Price' } ]\n *   @Semantics.amount.currencyCode: 'CurrencyCode'\n *   TotalPrice;\n *\n *   @UI.lineItem: [ { position: 30, label: 'Status' } ]\n *   @UI.selectionField: [ { position: 10 } ]\n *   Status;\n * }\n * ====================================================================\n */"
    }
  ],
  "content": "<h2 id=\"section-ZPRAC_CDS_MDX_EXT-cds-source-code\">CDS Source Code</h2>\n<pre><code class=\"language-abap\">/*\n * ====================================================================\n * BASE CDS VIEW (with extension enabled)\n * ====================================================================\n */\n@AbapCatalog.sqlViewName: 'ZPRAC_SQL_MDXEXT'\n@AbapCatalog.compiler.compareFilter: true\n@AbapCatalog.preserveKey: true\n@AccessControl.authorizationCheck: #CHECK\n@EndUserText.label: 'Practice CDS - Base view for Metadata Extension'\n@Metadata.allowExtensions: true\ndefine view ZPRAC_CDS_MDX_EXT\n  as select from /dmo/travel\n{\n  @Environment.systemField: #CLIENT\n  key client            as Client,\n  key travel_id         as TravelId,\n      agency_id         as AgencyId,\n      customer_id       as CustomerId,\n      @Semantics.date: true\n      begin_date        as BeginDate,\n      @Semantics.date: true\n      end_date          as EndDate,\n      @Semantics.amount.currencyCode: 'CurrencyCode'\n      booking_fee       as BookingFee,\n      @Semantics.amount.currencyCode: 'CurrencyCode'\n      total_price       as TotalPrice,\n      currency_code     as CurrencyCode,\n      description       as Description,\n      status            as Status\n}\n\n/*\n * ====================================================================\n * METADATA EXTENSION (separate artifact, not inside the view)\n *\n * Create as a separate file: ZPRAC_CDS_MDX_EXT.asr\n * \n * @Metadata.layer: #CORE\n * annotate view ZPRAC_CDS_MDX_EXT with\n * {\n *   @UI.facet: [\n *     {\n *       id: 'TravelFacet',\n *       purpose: #STANDARD,\n *       type: #IDENTIFICATION_REFERENCE,\n *       label: 'Travel Details'\n *     }\n *   ]\n *   TravelId;\n *\n *   @UI.lineItem: [ { position: 10, label: 'Travel ID' } ]\n *   @UI.identification: [ { position: 10 } ]\n *   TravelId;\n *\n *   @UI.lineItem: [ { position: 20, label: 'Total Price' } ]\n *   @Semantics.amount.currencyCode: 'CurrencyCode'\n *   TotalPrice;\n *\n *   @UI.lineItem: [ { position: 30, label: 'Status' } ]\n *   @UI.selectionField: [ { position: 10 } ]\n *   Status;\n * }\n * ====================================================================\n */</code></pre>",
  "tags": [
    "cds-views"
  ],
  "details": "<p class=\"mb-2\">WHAT IS A METADATA EXTENSION?</p><p class=\"mb-2\">A separate CDS artifact that adds UI/analytics annotations on top</p><p class=\"mb-2\">of an existing CDS view WITHOUT modifying the view itself.</p><p class=\"mb-2\">BENEFITS:</p><p class=\"mb-2\">✓ The core data model stays clean (no UI annotations mixed in)</p><p class=\"mb-2\">✓ Different consumers can have different metadata extensions</p><p class=\"mb-2\">✓ Upgrade-safe — core view doesn't change when UI requirements change</p><p class=\"mb-2\">✓ Multiple extensions can be stacked</p><p class=\"mb-2\">HOW IT WORKS:</p><p class=\"mb-2\">1. The base view has @Metadata.allowExtensions: true</p><p class=\"mb-2\">2. A separate .asr file (Metadata Extension) uses ANNOTATE VIEW</p><p class=\"mb-2\">3. The extension is activated independently</p><p class=\"mb-2\">4. SAP S/4HANA uses this extensively for Fiori apps</p><p class=\"mb-2\">SYNTAX OF METADATA EXTENSION:</p><p class=\"mb-2\">@Metadata.layer: #CORE         // Who owns this: #CORE / #CUSTOMER / #PARTNER</p><p class=\"mb-2\">annotate view ZPRAC_CDS_MDX_EXT with</p><p class=\"mb-2\">{</p><p class=\"mb-2\">@UI.facet        : [ { ... } ]</p><p class=\"mb-2\">@UI.lineItem     : [ { position: 10 } ]</p><p class=\"mb-2\">FieldName;</p><p class=\"mb-2\">}</p><p class=\"mb-2\">IMPORTANT:</p><p class=\"mb-2\">Base views with @Metadata.allowExtensions: true should still include</p><p class=\"mb-2\">all @Semantics annotations on fields — the metadata extension only adds</p><p class=\"mb-2\">UI-specific annotations, not semantic/analytic annotations.</p>",
  "cdsViewName": "ZPRAC_CDS_MDX_EXT",
  "sqlViewName": "ZPRAC_SQL_MDXEXT",
  "topic": "@Metadata.allowExtensions + Metadata Extension artifact"
}