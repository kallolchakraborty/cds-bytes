{
  "id": "ZPRAC_CDS_DATE_FUNC",
  "title": "Date and time manipulation in CDS",
  "phase": 5,
  "phaseName": "Built-in Functions",
  "category": "Built-in Functions",
  "subcategory": "Date/Time",
  "language": "abap",
  "description": "Date and time manipulation in CDS",
  "sections": [
    {
      "title": "CDS Source Code",
      "language": "abap",
      "codeBlock": "@AbapCatalog.sqlViewName: 'ZPRAC_SQL_DATE'\n@AbapCatalog.compiler.compareFilter: true\n@AbapCatalog.preserveKey: true\n@AccessControl.authorizationCheck: #CHECK\n@EndUserText.label: 'Practice CDS - Date/time functions (DATS, TSTMP)'\ndefine view ZPRAC_CDS_DATE_FUNC\n  as select from /dmo/travel\n{\n  key travel_id         as TravelId,\n      @Semantics.date: true\n      begin_date        as BeginDate,\n      @Semantics.date: true\n      end_date          as EndDate,\n      @Semantics.date: true\n      createdat         as CreatedAt,\n\n  /* ── Date Arithmetic ── */\n      @Semantics.date: true\n      dats_add_days(begin_date, 30)                 as BeginPlus30,\n      @Semantics.date: true\n      dats_add_months(begin_date, 3)                as BeginPlus3Months,\n      @Semantics.date: true\n      dats_add_days(end_date, -7)                   as EndMinus7,\n\n  /* ── Date Difference ── */\n      dats_days_between(begin_date, end_date)       as TripDurationDays,\n\n  /* ── Date Validation ── */\n      dats_is_valid(begin_date)                     as IsBeginValid,\n      dats_is_valid(end_date)                       as IsEndValid,\n\n  /* ── Timestamp Conversion ── */\n      @Semantics.date: true\n      tstmp_to_dats(createdat)                     as CreatedDate,\n      tstmp_to_tims(createdat)                     as CreatedTime,\n\n  /* ── Current System Values ── */\n      @Semantics.date: true\n      abap_current_date( )                          as Today,\n      abap_current_time( )                          as NowTime,\n\n  /* ── Validation Logic ── */\n      case\n        when end_date < abap_current_date( ) then 'Past'\n        when end_date = abap_current_date( ) then 'Today'\n        else 'Future'\n      end                                           as TripTiming\n}"
    }
  ],
  "content": "<h2 id=\"section-ZPRAC_CDS_DATE_FUNC-cds-source-code\">CDS Source Code</h2>\n<pre><code class=\"language-abap\">@AbapCatalog.sqlViewName: 'ZPRAC_SQL_DATE'\n@AbapCatalog.compiler.compareFilter: true\n@AbapCatalog.preserveKey: true\n@AccessControl.authorizationCheck: #CHECK\n@EndUserText.label: 'Practice CDS - Date/time functions (DATS, TSTMP)'\ndefine view ZPRAC_CDS_DATE_FUNC\n  as select from /dmo/travel\n{\n  key travel_id         as TravelId,\n      @Semantics.date: true\n      begin_date        as BeginDate,\n      @Semantics.date: true\n      end_date          as EndDate,\n      @Semantics.date: true\n      createdat         as CreatedAt,\n\n  /* ── Date Arithmetic ── */\n      @Semantics.date: true\n      dats_add_days(begin_date, 30)                 as BeginPlus30,\n      @Semantics.date: true\n      dats_add_months(begin_date, 3)                as BeginPlus3Months,\n      @Semantics.date: true\n      dats_add_days(end_date, -7)                   as EndMinus7,\n\n  /* ── Date Difference ── */\n      dats_days_between(begin_date, end_date)       as TripDurationDays,\n\n  /* ── Date Validation ── */\n      dats_is_valid(begin_date)                     as IsBeginValid,\n      dats_is_valid(end_date)                       as IsEndValid,\n\n  /* ── Timestamp Conversion ── */\n      @Semantics.date: true\n      tstmp_to_dats(createdat)                     as CreatedDate,\n      tstmp_to_tims(createdat)                     as CreatedTime,\n\n  /* ── Current System Values ── */\n      @Semantics.date: true\n      abap_current_date( )                          as Today,\n      abap_current_time( )                          as NowTime,\n\n  /* ── Validation Logic ── */\n      case\n        when end_date < abap_current_date( ) then 'Past'\n        when end_date = abap_current_date( ) then 'Today'\n        else 'Future'\n      end                                           as TripTiming\n}</code></pre>",
  "tags": [
    "cds-views"
  ],
  "details": "<p class=\"mb-2\">Available date/time functions in ABAP CDS:</p><p class=\"mb-2\">┌────────────────────────┬────────────────────────────────────────┐</p><p class=\"mb-2\">│ Function               │ Description                            │</p><p class=\"mb-2\">├────────────────────────┼────────────────────────────────────────┤</p><p class=\"mb-2\">│ DATS_ADD_DAYS(d, n)    │ Add days to date                      │</p><p class=\"mb-2\">│ DATS_ADD_MONTHS(d, n)  │ Add months to date                    │</p><p class=\"mb-2\">│ DATS_DAYS_BETWEEN(d1,d2)│ Days between two dates                │</p><p class=\"mb-2\">│ DATS_IS_VALID(d)       │ Check if date is valid (1=valid)       │</p><p class=\"mb-2\">│ TSTMP_TO_DATS(t)       │ Timestamp → date                      │</p><p class=\"mb-2\">│ TSTMP_TO_TIMS(t)       │ Timestamp → time                      │</p><p class=\"mb-2\">│ ABAP_NOW( )            │ Current timestamp (TSTMP)              │</p><p class=\"mb-2\">│ ABAP_CURRENT_DATE( )   │ Current date (DATS)                    │</p><p class=\"mb-2\">│ ABAP_CURRENT_TIME( )   │ Current time (TIMS)                    │</p><p class=\"mb-2\">│ TSTMP_SECONDS_BETWEEN  │ Seconds between two timestamps          │</p><p class=\"mb-2\">└────────────────────────┴────────────────────────────────────────┘</p><p class=\"mb-2\">DATA TYPE REFERENCE:</p><p class=\"mb-2\">abap.dats  → Date (YYYYMMDD), 8 chars</p><p class=\"mb-2\">abap.tims  → Time (HHMMSS), 6 chars</p><p class=\"mb-2\">abap.tstmp → Timestamp (YYYYMMDDHHMMSS), 14 chars (without dec.)</p>",
  "cdsViewName": "ZPRAC_CDS_DATE_FUNC",
  "sqlViewName": "ZPRAC_SQL_DATE",
  "topic": "Date/time functions — DATS, TSTMP, and system timestamps"
}